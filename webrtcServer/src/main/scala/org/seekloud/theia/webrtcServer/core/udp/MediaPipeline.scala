package org.seekloud.theia.webrtcServer.core.udp

import java.net.InetSocketAddress
import java.nio.channels.DatagramChannel

import org.seekloud.theia.shared.rtp.RtpClient
import org.seekloud.theia.webrtcServer.common.AppSettings

/**
  * Created by sky
  * Date on 2019/7/18
  * Time at 13:42
  */
object MediaPipeline {
//  val channel4push = DatagramChannel.open()
//  channel4push.socket().setReuseAddress(true)
//  channel4push.socket().bind(new InetSocketAddress(AppSettings.magicIp, AppSettings.magicPushPort))
//  val target4push = new InetSocketAddress(AppSettings.rtpServerIp, AppSettings.rtpServerPushPort)
//
//  val channel4pull = DatagramChannel.open()
//  channel4pull.socket().setReuseAddress(true)
//  channel4pull.socket().bind(new InetSocketAddress(AppSettings.magicIp, AppSettings.magicPullPort))
//  val target4pull = new InetSocketAddress(AppSettings.rtpServerIp, AppSettings.rtpServerPullPort)
}
